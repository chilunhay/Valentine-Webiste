# Valentine Website

This is a code bundle for Valentine Website. The original project is available at https://www.figma.com/design/j8rNwsTwaHI7TFwRqFSPQj/Valentine-Website.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the frontend server.

Run `cd server - npm run dev` to start the backend server.

Password for Login page "17052025"
